package com.ROTH.Framwork;

import java.awt.Graphics2D;
import java.awt.Image;
import java.util.ArrayList;

public class RenderingCheckerThread extends Thread {
	
	public ArrayList<ArrayList<Image>> map = null;
	public ArrayList<ArrayList<Image>> rendermap = null;
	public int Xoffset = 0;
	public int Yoffset = 0;
	public ROTHLevelActor user = null;

	
	public void run (){
		
		int Xscale = 40;
		int Yscale = 40;
		
		while(true){
			
	
			
			if(map == null || rendermap == null || Yoffset == 0 || Xoffset == 0 || user == null){
				
			}else{
				
				rendermap = new ArrayList<ArrayList<Image>>();
				
				for(int x = 0; x<500; x++){
					
					ArrayList<Image> temp = new ArrayList<Image>();
					
					for(int y = 0; y < 500; y++){
				
					//	if( (user.getPosX() - x*Xscale+Xoffset ) > 10*Xscale  ){
							
						//	Xoffset+=Xscale;
							
							
							
						//}else{
							
							temp.add(map.get(x).get(y));
							
						//}
					
					
					}
					rendermap.add(temp);
				}
		
			}
		}
	}
	
	

}
